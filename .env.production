# Used by `npm run build` (the Capacitor/Android app). The phone reaches the
# backend over Wi-Fi via the PC's LAN IP — 127.0.0.1 on the phone would be the
# phone itself. Update if the PC's IP changes (check with `ipconfig`).
VITE_API_BASE=http://192.168.1.82:8100
